#pragma once

// EXTERNAL GEAR RATIOS
constexpr float CATA_RATIO = (float)1/3;

// CONSTANTS
constexpr float TURN_CONST = 0.6;          // Turning speed multiplier
constexpr float DEFAULT_CONTROLLER_DEADZONE = 0.1; // Controller deadzone (cross)
constexpr float STICK_CURVE_GAIN = 2;      // Joystick input curve gain